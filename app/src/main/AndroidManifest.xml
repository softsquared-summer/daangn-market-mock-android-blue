<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.softsquared.daangnmarket">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".src.ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".src.uploadProduct.UploadProductActivity"></activity>
        <activity android:name=".src.appSetting.AppSettingActivity" />
        <activity android:name=".src.location.LocationActivity" />
        <activity android:name=".src.join.JoinActivity" />
        <activity android:name=".src.login.LoginActivity" />
        <activity android:name=".src.start.StartActivity" />
        <activity android:name=".src.product.ProductActivity" />
        <activity android:name=".src.main.MainActivity" />
        <activity
            android:name=".src.splash.SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>